<%= form_with(model: contribucion) do |form| %>
  <% if contribucion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contribucion.errors.count, "error") %> prohibited this contribucion from being saved:</h2>

      <ul>
        <% contribucion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
